@using TownOfStettler.Models.Exceptions

@{
    ViewData["Title"] = "New Device Entry";
}
<h1> Add A New Hardware Device to the Database</h1>
<h4>New Device from Invoice</h4>
<hr />

@if (ViewData["Exceptions"] != null)
{
    <ul>
        @foreach (Exception sub in (ViewData["Exceptions"] as ValidationException).SubExceptions)
        {
            <li>@sub.Message</li>
        }
    </ul>
}

<div class="row"> 
    <div class="col-md-4">
        <form asp-action="EnterNewHardwareDevice">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>


            <div class="form-group">
                <label class="control-label">Owner/Location ID #</label>
                <input name="ownerLocationName" class="form-control" placeholder="int not null" />
            </div>
            <p></p>
            <div class="form-group">
                <label class="control-label">Owner/Location Address</label>
                <input nameownerAddress class="form-control" placeholder="String not null" />
            </div>
            <p></p>
            <div class="form-group">
                <label class="control-label">Owner/Location Phone Number</label>
                <input name="ownerPhoneNumber" class="form-control" placeholder="String null" />
            </div>
            <p></p>


            <div class="form-group">
                <label class="control-label">TOS Number for New Hardware</label>
                <input name="hrdwreTOSnum" class="form-control" placeholder="String not null" />
            </div>
            <p></p>
            <div class="form-group">
                <label class="control-label">Other Hardware Type (Mouse/Keyboard/Speakers/Etc)</label>
                <input name="hrdwreType" class="form-control" placeholder="String not null" />
            </div>
            <p></p>
            <div class="form-group">
                <input type="submit" value="This is the GO button!" class="btn btn-primary" />
            </div>













        </form>
    </div>
</div>